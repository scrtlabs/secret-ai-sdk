[tool.poetry]
name = "claive-sdk"
version = "0.1.0"
description = "Claive AI Python SDK"
authors = ["Alex H <alexh@scrtlabs.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
